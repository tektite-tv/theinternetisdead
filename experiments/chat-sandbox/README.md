# chat-sandbox

Standalone chat/command sandbox. This folder contains the chat iframe/page and the JavaScript that drives local chat behavior outside the main site shell.

Key contents: `chat-sandbox.html`, `index.html`, `main-chat-sandbox.js`.
