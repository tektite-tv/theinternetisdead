# theinternetisdead-main

Root site shell for theinternetisdead.org. This is the main entry point and top-level home for pages, games, experiments, media, and helper scripts.

Key contents: `404.html`, `CNAME`, `index.html`.
