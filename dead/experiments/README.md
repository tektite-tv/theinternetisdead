# experiments

Landing area for experimental pages and prototypes. Each subfolder here is its own self-contained weird little test chamber, because apparently one site was not enough.

Key contents: `experiments.html`, `index.html`.
