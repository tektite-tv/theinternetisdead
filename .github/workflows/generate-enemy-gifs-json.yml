name: Generate enemy-gifs.json

on:
  push:
    paths:
      - "games/shooter-game/assets/enemy-gifs/**"
      - ".github/workflows/generate-enemy-gifs-json.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-enemy-gifs-json:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Generate enemy-gifs.json
        shell: bash
        run: |
          mkdir -p games/shooter-game/assets

          find games/shooter-game/assets/enemy-gifs \
            -maxdepth 1 \
            -type f \
            \( -iname "*.gif" -o -iname "*.webp" -o -iname "*.png" -o -iname "*.jpg" -o -iname "*.jpeg" \) \
            -printf '%f\n' \
            | sort \
            | jq -R -s 'split("\n") | map(select(length > 0))' \
            > games/shooter-game/assets/enemy-gifs.json

          echo "Generated games/shooter-game/assets/enemy-gifs.json:"
          cat games/shooter-game/assets/enemy-gifs.json

      - name: Commit generated JSON
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Generate enemy-gifs.json"
          file_pattern: games/shooter-game/assets/enemy-gifs.json
